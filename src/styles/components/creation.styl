body
    #creation
        width 100%
        height 1000px
        background linear-gradient(to right, #F76B1C, #FAD961) fixed
        text-align center
        padding-top 40px
        h1
            color white
            font-size 24px
        h2
            color white            
            font-size 18px
            margin-top 10px